// "use strict"; // kod obicnih JS moze se birati "use strict", KOD MODULA JE UVEK UPALJENA TA DIREKTIVA!!

let x = 4; // pomocna promenljiva
let y = 8; // pomocna promenljiva

// ........

let rezultat = x + y;

let racunaj = () => {
    document.body.innerHTML += `<p>${rezultat}</p>`;
}

